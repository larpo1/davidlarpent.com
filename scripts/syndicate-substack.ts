/**
 * Substack Syndication Script
 *
 * TODO: Implement Substack cross-posting functionality
 *
 * This script will:
 * 1. Read a published markdown post from src/content/posts/
 * 2. Convert it to Substack-compatible format
 * 3. Use Substack's API to create/import the post
 * 4. Add a canonical link back to davidlarpent.com
 * 5. Optionally send as newsletter to subscribers
 *
 * Usage (planned):
 *   npm run syndicate:substack -- --post="post-slug"
 *
 * Configuration needed:
 * - SUBSTACK_API_KEY (environment variable)
 * - SUBSTACK_PUBLICATION_URL (environment variable)
 *
 * References:
 * - Substack API docs (when available)
 * - Consider using Substack's import feature as fallback
 */

export {};
